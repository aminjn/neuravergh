
  # Admin panel with sidebar (Copy)

  This is a code bundle for Admin panel with sidebar (Copy). The original project is available at https://www.figma.com/design/2lfEOWsFdoPV61OKWNCE5D/Admin-panel-with-sidebar--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  